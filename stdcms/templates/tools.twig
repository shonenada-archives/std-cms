{%- macro menu_url(menu) -%}
  {%- if menu.type == 3 %}
{{ menu.outside_url }}
  {%- elseif menu.type == 2 %}
{{ siteUrl('menu/' ~ menu.id ~ '/list') }}
  {%- elseif menu.type == 1 %}
{{ siteUrl('menu/' ~ menu.id ~ '/show') }}
  {%- elseif menu.type == 0 %}
{{ 'javascript:void(0);' }}
  {%- endif %}
{%- endmacro -%}
